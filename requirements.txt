# Python dependencies for the videoai pipeline.
# Generated from a codebase audit: only packages actually imported by code
# under src/ are listed. Install with:
#   pip install -r requirements.txt

# --- TTS core (src/services/tts.py, tts_dsp.py) ---
soundfile
librosa
numpy
scipy
torch
torchaudio

# --- Local TTS engine (Pocket-TTS narrator; main branch audio) ---
pocket-tts           # import pocket_tts
# chatterbox-tts is used only on the wizdom branch (cloned persona voices):
#   chatterbox-tts       # import chatterbox

# --- HTTP / asset fetching (src/services/asset_fetcher.py) ---
requests
urllib3
ddgs
wikipedia

# --- Topic agent scraping (src/services/topic_agent/) ---
beautifulsoup4    # HTML index/scrape parsing
lxml              # strict XML/RSS/Atom parse via bs4 "xml" parser
feedparser        # optional: pleasant RSS/Atom parsing; agent uses stdlib+bs4 today

# --- LLM APIs (src/services/llm.py, topic_generator.py) ---
groq
ollama
google-genai
playwright
pytesseract

# --- YouTube upload (src/services/youtube_upload.py) ---
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# --- Video assembly (src/services/video_assembler.py) ---
moviepy
Pillow

# --- Config / env (src/config/settings.py) ---
python-dotenv