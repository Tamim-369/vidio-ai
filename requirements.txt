# Python dependencies for the videoai pipeline.
# Generated from a codebase audit: only packages actually imported by code
# under src/ are listed. Install with:
#   pip install -r requirements.txt
#
# NOTE: chatterbox-tts declares strict transitive pins (librosa==0.11.0,
# torch==2.6.0) that this repo intentionally overrides (librosa 1.0, torch
# 2.13). If pip hits a resolution conflict, install the TTS block separately
# with --no-deps / --no-build-isolation.

# --- TTS core (src/services/tts.py, tts_dsp.py, src/voice_tests/) ---
soundfile
librosa
numpy
scipy
torch
torchaudio

# --- Local TTS models (src/voices_to_clone/, src/voice_tests/) ---
chatterbox-tts       # import chatterbox
pocket-tts           # import pocket_tts
# qwen-tts            # optional: only used by voice_tests/test_qwen_0.6b_base.py in its own .venv-qwen

# --- HTTP / asset fetching (src/services/asset_fetcher.py) ---
requests
urllib3
ddgs
wikipedia

# --- LLM APIs (src/services/llm.py, topic_generator.py) ---
groq
ollama
google-genai
playwright
pytesseract

# --- YouTube upload (src/services/youtube_upload.py) ---
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# --- Video assembly (src/services/video_assembler.py) ---
moviepy
Pillow

# --- Config / env (src/config/settings.py) ---
python-dotenv